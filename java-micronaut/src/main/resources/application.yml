micronaut:
  application:
    name: javaMicronaut
  server:
    port: 8081
  router:
    static-resources:
      swagger:
        paths: classpath:static/docs
        mapping: /docs/**

netty:
  default:
    allocator:
      max-order: 3